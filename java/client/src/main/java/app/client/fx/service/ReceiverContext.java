package app.client.fx.service;

public interface ReceiverContext {
    //TODO: this interface need to provide methods for updating the current-message field
    /*
       if (message != null) {
           Platform.runLater(() -> {
               if (message.startsWith(Constants.MEMBERS_COMMAND)) {
                   this.latestMessage.setValue(null);
               }

               this.latestMessage.setValue(message);
           });
       }
     */
}
